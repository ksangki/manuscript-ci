# Manuscript CI 기여 가이드

[English](CONTRIBUTING.md) | **한국어**

Manuscript CI는 계속 **보수적인 도구**로 남아야 한다. 새로운 기능은 원고를 전형적인 AI 문체로 바꾸지 않으면서, 저자가 실제 결함을 더 잘 찾도록 도울 때만 가치가 있다.

## 원칙

- 동점이면 원문이 이긴다.
- 사실, 인용, 출처, 저자 경험, 숫자를 만들어내지 않는다.
- 원고를 조용히 자동 수정하는 것보다 report-only 자동화를 우선한다.
- 특정 AI 공급자 adapter는 선택 사항이어야 하며, 코어가 특정 업체를 필수로 요구하지 않게 한다.
- 새로운 자동화를 추가하기 전에 exact replacement 안전장치와 의사결정 로직에 대한 테스트를 먼저 갖춘다.
- 문장을 더 예쁘게 만드는 기능보다 근거 규율, 장간 일관성, 저자 목소리 보호를 우선한다.

## 개발 환경

소스 checkout에서 바로 테스트하려면:

```bash
PYTHONPATH=src python -m pytest -q
PYTHONPATH=src python -m manuscript_ci.cli --help
```

일반적인 패키지 설치 방식으로 확인하려면:

```bash
pip install -e . pytest
pytest -q
manuscript-ci --help
```

## 변경을 제안할 때

가능하면 PR 또는 커밋 설명에 다음을 적는다.

- 어떤 원고 리뷰 문제를 해결하는가
- 왜 기존 동작으로는 충분하지 않았는가
- 저자 목소리나 원문 안전성에 어떤 영향을 주는가
- 자동 수정을 추가한다면 어떤 조건에서 쓰기가 발생하는가
- 새 동작을 검증하는 테스트가 무엇인가

## 좋은 기능의 기준

좋은 기능:

- 같은 수치를 장마다 다르게 해석한 것을 찾는다.
- 근거보다 강한 인과 표현을 표시한다.
- 같은 개념을 여러 장이 처음처럼 설명하는 것을 찾는다.
- 수정안이 원문보다 확실히 나은 경우에만 적용한다.

신중해야 할 기능:

- 모든 문장을 더 유려하게 다시 쓰기
- 저자 허락 없이 자동으로 대량 수정하기
- 하나의 모델 점수만으로 수정안을 채택하기
- 특정 모델 공급자만 사용할 수 있게 코어를 묶기

## 문서

사용자 관점의 설명은 [`README_KO.md`](README_KO.md)와 [`GUIDE_KO.md`](GUIDE_KO.md), 에이전트 리뷰 원칙은 [`SKILL_KO.md`](SKILL_KO.md)를 참고한다.
